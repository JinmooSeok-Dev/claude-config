# Credentials & secrets
.credentials.json

# Local settings (personal overrides)
settings.local.json

# Session data (large, transient)
history.jsonl
cache/
debug/
file-history/
paste-cache/
plans/
projects/
session-env/
shell-snapshots/
stats-cache.json
statsig/
tasks/
telemetry/
todos/

# Transient state
.update.lock
mcp-needs-auth-cache.json
backups/
sessions/

# Plugins (installed separately, large)
plugins/

# 외부 plugin (별도 marketplace로 설치: /plugin install autoresearch@jinmoo-personal)
commands/autoresearch.md
hooks/autoresearch-context.sh
skills/autoresearch
