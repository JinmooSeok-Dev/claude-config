# Credentials & secrets
.credentials.json

# Local settings (personal overrides)
settings.local.json

# Session data (large, transient)
history.jsonl
cache/
debug/
file-history/
paste-cache/
plans/
projects/
session-env/
shell-snapshots/
stats-cache.json
statsig/
tasks/
telemetry/
todos/

# Transient state
.update.lock
mcp-needs-auth-cache.json
backups/
sessions/

# Plugins (installed separately, large)
plugins/

# 외부 plugin (별도 marketplace로 설치: /plugin install autoresearch@jinmoo-personal)
commands/autoresearch.md
hooks/autoresearch-context.sh
skills/autoresearch

# 외부 도구 심볼릭 링크 (~/.git-ai 등 머신 종속)
skills/ask
skills/git-ai-search
skills/prompt-analysis

# Claude Code 자체 생성 머신 종속 파일
policy-limits.json
